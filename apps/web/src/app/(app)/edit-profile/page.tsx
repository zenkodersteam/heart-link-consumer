import { EditProfile } from '@/components/account/edit-profile';

export const metadata = { title: 'Your profile · HeartLink' };

export default function EditProfilePage() {
  return <EditProfile />;
}
