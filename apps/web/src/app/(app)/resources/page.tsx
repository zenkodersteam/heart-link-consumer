import { Resources } from '@/components/resources/resources';

export const metadata = { title: 'Resources & Support · HeartLink' };

export default function ResourcesPage() {
  return <Resources />;
}
