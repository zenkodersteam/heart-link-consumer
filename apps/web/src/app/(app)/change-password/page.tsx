import { ChangePassword } from '@/components/account/change-password';

export const metadata = { title: 'Change password · HeartLink' };

export default function ChangePasswordPage() {
  return <ChangePassword />;
}
